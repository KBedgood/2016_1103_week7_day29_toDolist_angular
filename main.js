// The ngCloak directive is used to prevent the Angular html template from being briefly displayed 
// by the browser in its raw (uncompiled) form while your application is loading. Use this directive to 
// avoid the undesirable flicker effect caused by the html template display.

[ng\:cloak], [ng-cloak], [data-ng-cloak], [x-ng-cloak], .ng-cloak, .x-ng-cloak {
  display: none !important;
}

(function() {
    'use strict';

// Save all inventory data in an array:
    angular
        .module('stabList', [])
    

})();
